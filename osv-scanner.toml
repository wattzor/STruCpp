# osv-scanner suppression baseline for strucpp = our VEX "not affected"
# decisions from the SBOM & Vulnerability Report. STruCpp is distributed as a
# self-contained binary; only the chevrotain runtime subtree ships. Build/test
# tooling is component_not_present; lodash-es (via chevrotain) is not-in-path.
# The real shipped runtime subtree is left UNSUPPRESSED so a future advisory in
# it surfaces for triage.
#
# Regenerated from a live osv-scanner scan of the CycloneDX SBOM.
# Each entry expires (ignoreUntil) so suppressions are re-reviewed quarterly.
# Full rationale: security/<YYYY-MM>/STruCpp-Security-Report.md.

[[IgnoredVulns]]
id = "GHSA-22p9-wv53-3rq4"
ignoreUntil = "2026-10-01T00:00:00Z"
reason = "VEX not_affected [component_not_present]: development / build / test tooling (or companion VS Code extension dependency), not part of the distributed compiler binary — only the chevrotain runtime subtree ships [linkify-it] — see security report"

[[IgnoredVulns]]
id = "GHSA-23c5-xmqv-rm74"
ignoreUntil = "2026-10-01T00:00:00Z"
reason = "VEX not_affected [component_not_present]: development / build / test tooling (or companion VS Code extension dependency), not part of the distributed compiler binary — only the chevrotain runtime subtree ships [minimatch] — see security report"

[[IgnoredVulns]]
id = "GHSA-23hp-3jrh-7fpw"
ignoreUntil = "2026-10-01T00:00:00Z"
reason = "VEX not_affected [component_not_present]: development / build / test tooling (or companion VS Code extension dependency), not part of the distributed compiler binary — only the chevrotain runtime subtree ships [tar] — see security report"

[[IgnoredVulns]]
id = "GHSA-25h7-pfq9-p65f"
ignoreUntil = "2026-10-01T00:00:00Z"
reason = "VEX not_affected [component_not_present]: development / build / test tooling (or companion VS Code extension dependency), not part of the distributed compiler binary — only the chevrotain runtime subtree ships [flatted] — see security report"

[[IgnoredVulns]]
id = "GHSA-2g4f-4pwh-qvx6"
ignoreUntil = "2026-10-01T00:00:00Z"
reason = "VEX not_affected [component_not_present]: development / build / test tooling (or companion VS Code extension dependency), not part of the distributed compiler binary — only the chevrotain runtime subtree ships [ajv] — see security report"

[[IgnoredVulns]]
id = "GHSA-35p6-xmwp-9g52"
ignoreUntil = "2026-10-01T00:00:00Z"
reason = "VEX not_affected [component_not_present]: development / build / test tooling (or companion VS Code extension dependency), not part of the distributed compiler binary — only the chevrotain runtime subtree ships [undici] — see security report"

[[IgnoredVulns]]
id = "GHSA-3jxr-9vmj-r5cp"
ignoreUntil = "2026-10-01T00:00:00Z"
reason = "VEX not_affected [component_not_present]: development / build / test tooling (or companion VS Code extension dependency), not part of the distributed compiler binary — only the chevrotain runtime subtree ships [brace-expansion] — see security report"

[[IgnoredVulns]]
id = "GHSA-3ppc-4f35-3m26"
ignoreUntil = "2026-10-01T00:00:00Z"
reason = "VEX not_affected [component_not_present]: development / build / test tooling (or companion VS Code extension dependency), not part of the distributed compiler binary — only the chevrotain runtime subtree ships [minimatch] — see security report"

[[IgnoredVulns]]
id = "GHSA-3v7f-55p6-f55p"
ignoreUntil = "2026-10-01T00:00:00Z"
reason = "VEX not_affected [component_not_present]: development / build / test tooling (or companion VS Code extension dependency), not part of the distributed compiler binary — only the chevrotain runtime subtree ships [picomatch] — see security report"

[[IgnoredVulns]]
id = "GHSA-48c2-rrv3-qjmp"
ignoreUntil = "2026-10-01T00:00:00Z"
reason = "VEX not_affected [component_not_present]: development / build / test tooling (or companion VS Code extension dependency), not part of the distributed compiler binary — only the chevrotain runtime subtree ships [yaml] — see security report"

[[IgnoredVulns]]
id = "GHSA-4c8g-83qw-93j6"
ignoreUntil = "2026-10-01T00:00:00Z"
reason = "VEX not_affected [component_not_present]: development / build / test tooling (or companion VS Code extension dependency), not part of the distributed compiler binary — only the chevrotain runtime subtree ships [fast-uri] — see security report"

[[IgnoredVulns]]
id = "GHSA-4w7w-66w2-5vf9"
ignoreUntil = "2026-10-01T00:00:00Z"
reason = "VEX not_affected [component_not_present]: development / build / test tooling (or companion VS Code extension dependency), not part of the distributed compiler binary — only the chevrotain runtime subtree ships [vite] — see security report"

[[IgnoredVulns]]
id = "GHSA-52cp-r559-cp3m"
ignoreUntil = "2026-10-01T00:00:00Z"
reason = "VEX not_affected [component_not_present]: development / build / test tooling (or companion VS Code extension dependency), not part of the distributed compiler binary — only the chevrotain runtime subtree ships [js-yaml] — see security report"

[[IgnoredVulns]]
id = "GHSA-5xrq-8626-4rwp"
ignoreUntil = "2026-10-01T00:00:00Z"
reason = "VEX not_affected [component_not_present]: development / build / test tooling (or companion VS Code extension dependency), not part of the distributed compiler binary — only the chevrotain runtime subtree ships [vitest] — see security report"

[[IgnoredVulns]]
id = "GHSA-67mh-4wv8-2f99"
ignoreUntil = "2026-10-01T00:00:00Z"
reason = "VEX not_affected [component_not_present]: development / build / test tooling (or companion VS Code extension dependency), not part of the distributed compiler binary — only the chevrotain runtime subtree ships [esbuild] — see security report"

[[IgnoredVulns]]
id = "GHSA-6g55-p6wh-862q"
ignoreUntil = "2026-10-01T00:00:00Z"
reason = "VEX not_affected [component_not_present]: development / build / test tooling (or companion VS Code extension dependency), not part of the distributed compiler binary — only the chevrotain runtime subtree ships [postcss] — see security report"

[[IgnoredVulns]]
id = "GHSA-6v5v-wf23-fmfq"
ignoreUntil = "2026-10-01T00:00:00Z"
reason = "VEX not_affected [component_not_present]: development / build / test tooling (or companion VS Code extension dependency), not part of the distributed compiler binary — only the chevrotain runtime subtree ships [markdown-it] — see security report"

[[IgnoredVulns]]
id = "GHSA-7r86-cg39-jmmj"
ignoreUntil = "2026-10-01T00:00:00Z"
reason = "VEX not_affected [component_not_present]: development / build / test tooling (or companion VS Code extension dependency), not part of the distributed compiler binary — only the chevrotain runtime subtree ships [minimatch] — see security report"

[[IgnoredVulns]]
id = "GHSA-8x88-c5mf-7j5w"
ignoreUntil = "2026-10-01T00:00:00Z"
reason = "VEX not_affected [component_not_present]: development / build / test tooling (or companion VS Code extension dependency), not part of the distributed compiler binary — only the chevrotain runtime subtree ships [tar] — see security report"

[[IgnoredVulns]]
id = "GHSA-c2c7-rcm5-vvqj"
ignoreUntil = "2026-10-01T00:00:00Z"
reason = "VEX not_affected [component_not_present]: development / build / test tooling (or companion VS Code extension dependency), not part of the distributed compiler binary — only the chevrotain runtime subtree ships [picomatch] — see security report"

[[IgnoredVulns]]
id = "GHSA-f23m-r3pf-42rh"
ignoreUntil = "2026-10-01T00:00:00Z"
reason = "VEX not_affected [vulnerable_code_not_in_execute_path]: chevrotain uses lodash-es only for internal parser data structures; the vulnerable functions (_.template, _.unset/_.omit) are never reached by Structured Text input [lodash] — see security report"

[[IgnoredVulns]]
id = "GHSA-f886-m6hf-6m8v"
ignoreUntil = "2026-10-01T00:00:00Z"
reason = "VEX not_affected [component_not_present]: development / build / test tooling (or companion VS Code extension dependency), not part of the distributed compiler binary — only the chevrotain runtime subtree ships [brace-expansion] — see security report"

[[IgnoredVulns]]
id = "GHSA-fx2h-pf6j-xcff"
ignoreUntil = "2026-10-01T00:00:00Z"
reason = "VEX not_affected [component_not_present]: development / build / test tooling (or companion VS Code extension dependency), not part of the distributed compiler binary — only the chevrotain runtime subtree ships [vite] — see security report"

[[IgnoredVulns]]
id = "GHSA-g7r4-m6w7-qqqr"
ignoreUntil = "2026-10-01T00:00:00Z"
reason = "VEX not_affected [component_not_present]: development / build / test tooling (or companion VS Code extension dependency), not part of the distributed compiler binary — only the chevrotain runtime subtree ships [esbuild] — see security report"

[[IgnoredVulns]]
id = "GHSA-g8m3-5g58-fq7m"
ignoreUntil = "2026-10-01T00:00:00Z"
reason = "VEX not_affected [component_not_present]: development / build / test tooling (or companion VS Code extension dependency), not part of the distributed compiler binary — only the chevrotain runtime subtree ships [undici] — see security report"

[[IgnoredVulns]]
id = "GHSA-gvwx-54wh-qm9j"
ignoreUntil = "2026-10-01T00:00:00Z"
reason = "VEX not_affected [component_not_present]: development / build / test tooling (or companion VS Code extension dependency), not part of the distributed compiler binary — only the chevrotain runtime subtree ships [tar] — see security report"

[[IgnoredVulns]]
id = "GHSA-h67p-54hq-rp68"
ignoreUntil = "2026-10-01T00:00:00Z"
reason = "VEX not_affected [component_not_present]: development / build / test tooling (or companion VS Code extension dependency), not part of the distributed compiler binary — only the chevrotain runtime subtree ships [js-yaml] — see security report"

[[IgnoredVulns]]
id = "GHSA-hm92-r4w5-c3mj"
ignoreUntil = "2026-10-01T00:00:00Z"
reason = "VEX not_affected [component_not_present]: development / build / test tooling (or companion VS Code extension dependency), not part of the distributed compiler binary — only the chevrotain runtime subtree ships [undici] — see security report"

[[IgnoredVulns]]
id = "GHSA-hmw2-7cc7-3qxx"
ignoreUntil = "2026-10-01T00:00:00Z"
reason = "VEX not_affected [component_not_present]: development / build / test tooling (or companion VS Code extension dependency), not part of the distributed compiler binary — only the chevrotain runtime subtree ships [form-data] — see security report"

[[IgnoredVulns]]
id = "GHSA-jxxr-4gwj-5jf2"
ignoreUntil = "2026-10-01T00:00:00Z"
reason = "VEX not_affected [component_not_present]: development / build / test tooling (or companion VS Code extension dependency), not part of the distributed compiler binary — only the chevrotain runtime subtree ships [brace-expansion] — see security report"

[[IgnoredVulns]]
id = "GHSA-mh99-v99m-4gvg"
ignoreUntil = "2026-10-01T00:00:00Z"
reason = "VEX not_affected [component_not_present]: development / build / test tooling (or companion VS Code extension dependency), not part of the distributed compiler binary — only the chevrotain runtime subtree ships [brace-expansion] — see security report"

[[IgnoredVulns]]
id = "GHSA-mw96-cpmx-2vgc"
ignoreUntil = "2026-10-01T00:00:00Z"
reason = "VEX not_affected [component_not_present]: development / build / test tooling (or companion VS Code extension dependency), not part of the distributed compiler binary — only the chevrotain runtime subtree ships [rollup] — see security report"

[[IgnoredVulns]]
id = "GHSA-p88m-4jfj-68fv"
ignoreUntil = "2026-10-01T00:00:00Z"
reason = "VEX not_affected [component_not_present]: development / build / test tooling (or companion VS Code extension dependency), not part of the distributed compiler binary — only the chevrotain runtime subtree ships [undici] — see security report"

[[IgnoredVulns]]
id = "GHSA-p9ff-h696-f583"
ignoreUntil = "2026-10-01T00:00:00Z"
reason = "VEX not_affected [component_not_present]: development / build / test tooling (or companion VS Code extension dependency), not part of the distributed compiler binary — only the chevrotain runtime subtree ships [vite] — see security report"

[[IgnoredVulns]]
id = "GHSA-ph9p-34f9-6g65"
ignoreUntil = "2026-10-01T00:00:00Z"
reason = "VEX not_affected [component_not_present]: development / build / test tooling (or companion VS Code extension dependency), not part of the distributed compiler binary — only the chevrotain runtime subtree ships [tmp] — see security report"

[[IgnoredVulns]]
id = "GHSA-pr7r-676h-xcf6"
ignoreUntil = "2026-10-01T00:00:00Z"
reason = "VEX not_affected [component_not_present]: development / build / test tooling (or companion VS Code extension dependency), not part of the distributed compiler binary — only the chevrotain runtime subtree ships [undici] — see security report"

[[IgnoredVulns]]
id = "GHSA-q3j6-qgpj-74h6"
ignoreUntil = "2026-10-01T00:00:00Z"
reason = "VEX not_affected [component_not_present]: development / build / test tooling (or companion VS Code extension dependency), not part of the distributed compiler binary — only the chevrotain runtime subtree ships [fast-uri] — see security report"

[[IgnoredVulns]]
id = "GHSA-q8mj-m7cp-5q26"
ignoreUntil = "2026-10-01T00:00:00Z"
reason = "VEX not_affected [component_not_present]: development / build / test tooling (or companion VS Code extension dependency), not part of the distributed compiler binary — only the chevrotain runtime subtree ships [qs] — see security report"

[[IgnoredVulns]]
id = "GHSA-qx2v-qp2m-jg93"
ignoreUntil = "2026-10-01T00:00:00Z"
reason = "VEX not_affected [component_not_present]: development / build / test tooling (or companion VS Code extension dependency), not part of the distributed compiler binary — only the chevrotain runtime subtree ships [postcss] — see security report"

[[IgnoredVulns]]
id = "GHSA-r28c-9q8g-f849"
ignoreUntil = "2026-10-01T00:00:00Z"
reason = "VEX not_affected [component_not_present]: development / build / test tooling (or companion VS Code extension dependency), not part of the distributed compiler binary — only the chevrotain runtime subtree ships [postcss] — see security report"

[[IgnoredVulns]]
id = "GHSA-r292-9mhp-454m"
ignoreUntil = "2026-10-01T00:00:00Z"
reason = "VEX not_affected [component_not_present]: development / build / test tooling (or companion VS Code extension dependency), not part of the distributed compiler binary — only the chevrotain runtime subtree ships [tar] — see security report"

[[IgnoredVulns]]
id = "GHSA-r5fr-rjxr-66jc"
ignoreUntil = "2026-10-01T00:00:00Z"
reason = "VEX not_affected [vulnerable_code_not_in_execute_path]: chevrotain uses lodash-es only for internal parser data structures; the vulnerable functions (_.template, _.unset/_.omit) are never reached by Structured Text input [lodash] — see security report"

[[IgnoredVulns]]
id = "GHSA-rf6f-7fwh-wjgh"
ignoreUntil = "2026-10-01T00:00:00Z"
reason = "VEX not_affected [component_not_present]: development / build / test tooling (or companion VS Code extension dependency), not part of the distributed compiler binary — only the chevrotain runtime subtree ships [flatted] — see security report"

[[IgnoredVulns]]
id = "GHSA-v245-v573-v5vm"
ignoreUntil = "2026-10-01T00:00:00Z"
reason = "VEX not_affected [component_not_present]: development / build / test tooling (or companion VS Code extension dependency), not part of the distributed compiler binary — only the chevrotain runtime subtree ships [linkify-it] — see security report"

[[IgnoredVulns]]
id = "GHSA-v2hh-gcrm-f6hx"
ignoreUntil = "2026-10-01T00:00:00Z"
reason = "VEX not_affected [component_not_present]: development / build / test tooling (or companion VS Code extension dependency), not part of the distributed compiler binary — only the chevrotain runtime subtree ships [fast-uri] — see security report"

[[IgnoredVulns]]
id = "GHSA-v2wj-q39q-566r"
ignoreUntil = "2026-10-01T00:00:00Z"
reason = "VEX not_affected [component_not_present]: development / build / test tooling (or companion VS Code extension dependency), not part of the distributed compiler binary — only the chevrotain runtime subtree ships [vite] — see security report"

[[IgnoredVulns]]
id = "GHSA-v39h-62p7-jpjc"
ignoreUntil = "2026-10-01T00:00:00Z"
reason = "VEX not_affected [component_not_present]: development / build / test tooling (or companion VS Code extension dependency), not part of the distributed compiler binary — only the chevrotain runtime subtree ships [fast-uri] — see security report"

[[IgnoredVulns]]
id = "GHSA-v6wh-96g9-6wx3"
ignoreUntil = "2026-10-01T00:00:00Z"
reason = "VEX not_affected [component_not_present]: development / build / test tooling (or companion VS Code extension dependency), not part of the distributed compiler binary — only the chevrotain runtime subtree ships [vite] — see security report"

[[IgnoredVulns]]
id = "GHSA-vmf3-w455-68vh"
ignoreUntil = "2026-10-01T00:00:00Z"
reason = "VEX not_affected [component_not_present]: development / build / test tooling (or companion VS Code extension dependency), not part of the distributed compiler binary — only the chevrotain runtime subtree ships [tar] — see security report"

[[IgnoredVulns]]
id = "GHSA-vmh5-mc38-953g"
ignoreUntil = "2026-10-01T00:00:00Z"
reason = "VEX not_affected [component_not_present]: development / build / test tooling (or companion VS Code extension dependency), not part of the distributed compiler binary — only the chevrotain runtime subtree ships [undici] — see security report"

[[IgnoredVulns]]
id = "GHSA-vxpw-j846-p89q"
ignoreUntil = "2026-10-01T00:00:00Z"
reason = "VEX not_affected [component_not_present]: development / build / test tooling (or companion VS Code extension dependency), not part of the distributed compiler binary — only the chevrotain runtime subtree ships [undici] — see security report"

[[IgnoredVulns]]
id = "GHSA-w5hq-g745-h8pq"
ignoreUntil = "2026-10-01T00:00:00Z"
reason = "VEX not_affected [component_not_present]: development / build / test tooling (or companion VS Code extension dependency), not part of the distributed compiler binary — only the chevrotain runtime subtree ships [uuid] — see security report"

[[IgnoredVulns]]
id = "GHSA-w8wr-v893-vjvp"
ignoreUntil = "2026-10-01T00:00:00Z"
reason = "VEX not_affected [component_not_present]: development / build / test tooling (or companion VS Code extension dependency), not part of the distributed compiler binary — only the chevrotain runtime subtree ships [tar] — see security report"

[[IgnoredVulns]]
id = "GHSA-xxjr-mmjv-4gpg"
ignoreUntil = "2026-10-01T00:00:00Z"
reason = "VEX not_affected [vulnerable_code_not_in_execute_path]: chevrotain uses lodash-es only for internal parser data structures; the vulnerable functions (_.template, _.unset/_.omit) are never reached by Structured Text input [lodash-es] — see security report"
